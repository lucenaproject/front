# include "user"


type Query {
    me: User
    other: User
    milongas(id: Int, after: String, first: Int): [Milonga]
}

type User {
    id: ID
    name: String
}

type Milonga {
    id: ID
    name: String
    address: String
    habitues: [User]
}
